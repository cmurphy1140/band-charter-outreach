# Partner research pilot assignment

Task ID: `claude-partner-research`. Reserved September 11, 2026.
Status: **complete, verified and locally committed; Claude idle; no push**.

## Next assignment reservation — September 11, 2026

The partner pilot is closed. The coordinator has reserved the separate
[fictional proposal assignment](2026-09-11-proposal-example.md) with four new owned
files. The original worker branch/commit is retained. The clean checkout will be
switched to `codex/claude-proposal-example` at the combined checkpoint; see the
latest PROGRESS receipt and final startup prompt for verification. Claude is not
launched. Historical “no next task” statements below describe the earlier handoff.

## Overnight coordinator receipt

The user supplied Claude's final read-only shutdown confirmation on September 11.
Codex verified its clean checkout at `e171954439e97219491ff6fc2ff332700a1580ef`,
branch `feature/claude-partner-research`, with no stash. Claude reports no task
processes and is idle; its checkout remains unchanged. No new task is assigned.

Codex committed the preservation fix as `e6e493e2665dc6b1cd64b656844bdee4da9a8dbb`
and the reviewed MTC connection/corrections as
`f36bdf1fda895a3651a46b3b9b4f62ea21f971dc` on `codex/mtc-research-example`.
This ledger and shared/side-conversation documents are saved in the subsequent
documentation checkpoint. Fresh checks: 54 Python passes/four fixture skips,
15 Node passes, source/output reconciliation and scoped preservation checks.
Codex's verified demo server (PID 47009, port 8765) was stopped. No push or worktree
removal occurred. The original MTC pilot's budget-accounting finding remains open
as recorded; no new discovery was done to change that history.

The final whole-project audio presentation remains deferred. Nightly local packs
now follow [the wrap-up routine](../SESSION-WRAP-UP.md). All earlier uncommitted and
startup wording below is historical. Full paths, commit lookup, remaining untracked
files and tomorrow's single FAQ increment are in
[the overnight checkpoint](../PROGRESS.md#overnight-checkpoint--september-11-2026).

## Coordinator and starting state

- Coordinator: the main Codex task, **Troen Prospects Pipeline**
  (`01a08da2-45ab-7181-a80f-a887cb079101`) on the local Mac.
- Coordinator checkout: `/Users/connormurphy/Desktop/Projects/band-charter-outreach`;
  branch `codex/empty-scrape-preservation`.
- Worker: completed Claude Code Local session on this Mac, per its
  [handoff](claude-partner-research-handoff.md). It acknowledged the assignment and
  reports editing stopped after its commit. A prior cloud mismatch is recorded;
  no broader Desktop/cloud reliability claim is made.
- Worker checkout: `/Users/connormurphy/Desktop/Projects/band-charter-outreach-worktrees/claude-partner-research`.
- Worker branch: `feature/claude-partner-research`.
- Verified full starting commit: `39171e5fe873bca7fca3f05723a8fca9985f081b`.
  It adds the assignment and includes all fourteen reviewed setup documents.
  Worker delivery is `e171954439e97219491ff6fc2ff332700a1580ef`, whose exact parent
  is that starting commit. The worker checkout is clean at its delivery commit.
  Coordinator integration is `a7ceefc139c1f075060fc9f02a0ec70a86a975d3`; review did
  not edit or advance the worker checkout. Its ledger copy is a historical assignment.
- Policy: the September 11 [parallel workflow](../PARALLEL-WORKFLOW.md), one writer
  per assigned checkout/file set. This ledger is coordinator-owned.

## Outcome and exact write ownership

Research **one public student-performance tour provider** as a possible partner
and possible competitor, then draft a useful internal partner sheet for Troen's
**March 3, 2027** Carnegie proof of concept. This is research and Markdown drafting,
not a new app, automatic scraper, contact campaign or integrated demo change.

Claude owned exactly these four files during the pilot. After its completed
handoff, integrated-copy revisions are coordinator-owned; the worker checkout
stays unchanged:

| Allowed path | Required content |
|---|---|
| `docs/carnegie-hall/research/partner-pilot/PROFILE.md` | Verified organization identity, public services/territory and music-travel relevance, source-backed fit, potential conflicts, unknown relationship/interest and next useful action. |
| `docs/carnegie-hall/research/partner-pilot/SOURCES.md` | Source IDs, exact URLs, retrieval/review dates, page/section locator, individual supported claims and uncertainties; distinguish public facts from interpretation. |
| `docs/carnegie-hall/research/partner-pilot/PARTNER-SHEET-DRAFT.md` | Concise partner-facing language labeled **internal discussion draft, unapproved and not sent**, using only supported March 3 facts and clearly qualified responsibilities. |
| `docs/carnegie-hall/coordination/claude-partner-research-handoff.md` | Acknowledgement, base/final ref, files, checks, sources, spend, limitations, reusable lesson, proposed shared-doc changes and ready-for-review status. |

These are exact files, not ownership of their parent folders. They were absent at
reservation. No other worker is assigned to them. Codex reserves their integration
and all other tracked files, including AGENTS.md/CLAUDE.md, PROGRESS.md, this ledger,
plans/strategies, READMEs, dependency manifests, demo/build files, tests, legacy
pipeline, prospect data, logistics and existing outputs. Other tasks may be active;
Claude is not alone in the repository. Never revert their work or edit their
checkouts. Propose any necessary scope change in the handoff note before making it.

## Read-only inputs and resources

Read the current shared instructions, PROGRESS.md, PLAN.md, and strategies 01, 02,
03 and 05. Use the business-context source inventory and original logistics only
to understand the already documented offer. The existing Wando and Salem materials
are examples of evidence treatment, not text to attribute to this provider.

The coordinator copied and hash-verified exactly two credential-free cache files
at these ignored relative paths in the worker checkout, as independent read-only
files (not shared writable caches or symlinks):

| Input | SHA-256 |
|---|---|
| `data/raw/serpapi-validation/a2281fc6877577d586a2cfe66de5f5c4d21f9fa7.json` | `12c364f8dd185000db501fb44b5ba27b90c9d2662189a47ed160fa25b20941fb` |
| `data/raw/serpapi-validation/a2281fc6877577d586a2cfe66de5f5c4d21f9fa7.meta.json` | `3798d373a8fe43ae40fc4cc4644b7c56bc94d63811260fe44198f9ed517e9bc7` |

This is the September 11 SerpAPI discovery query `student music performance tours
Carnegie Hall`, not verified provider research or an up-to-date offer. Review the
underlying primary sources before accepting claims. No credential is transferred.
If the cache is unavailable, use permitted public browsing and report the missing
input; do not enter the coordinator's checkout to fetch secrets or shared caches.

- Paid/API budget: **zero new paid calls**, including SerpAPI, Firecrawl, Zyte and
  Apify. No trials, subscriptions or account changes. No direct SerpAPI calls are
  allocated; use the snapshot and ordinary permitted public browsing.
- Original public-source limit: **eight distinct first-party pages** for the
  selected provider. The aggregate review below counts all screened providers:
  eight readable pages and nine attempts, including a blocked page. Do not call
  this clean compliance with an eight-attempt cap. In any later separately
  authorized pass, count all attempted provider pages against one shared budget,
  including blocks and screened-out providers. No further research is authorized
  by this accounting clarification. Do not bypass blocked access.
- No outward messages, CRM imports, quote requests, signups, live refreshes,
  deployment or purchases. Published contacts do not establish permission to send.
- No server or port is assigned. Do not use or stop the coordinator's server at
  `127.0.0.1:8765`. No background process or scheduled task is needed.
- Markdown requires no new runtime/dependency. Any coordinator-installed worker
  environment is local to this worktree; never share writable `.venv` or
  `node_modules` with another checkout. Do not read or copy `.env`.

## Acceptance and completion

1. One correctly identified provider, supported public relevance and a balanced
   partner/competitor assessment. Do not infer interest, an existing Troen
   relationship, exclusive territory or superior performance.
2. Every material factual claim traces to a source ID and specific source passage
   or section. Names, roles, locations, dates and contacts retain explicit evidence;
   unavailable values remain unknown. Distinguish event type and planned/completed
   activity where relevant; search snippets alone do not establish claims.
3. A readable, useful partner draft consistent with March 3 only. Do not invent
   pricing, capacity, availability, commissions, bookings or approved responsibilities.
   Keep March 31 terms and internal costs out of the partner-facing draft. Use
   employee-reported attribution and the logistics and software developer role.
4. Preserve all existing files. Check the four outputs, local links and citations,
   `git diff --check`, and the complete changed/untracked path list. No application
   rebuild or production data refresh is required for Markdown research.
5. Record verification, source limits, zero API spend, a useful next action and
   a reusable lesson with its limits. If evidence is insufficient, report that
   clearly rather than fill gaps. The business does not need a homework exercise
   before this draft can be useful.

One coherent local conventional documentation commit of **only the four owned
files** is authorized after review and verification. Include a stable handoff note;
report the full base and final commit IDs in the completion message (a file cannot
contain its own final commit hash). Do not push, merge, rebase, pull, switch branches,
remove worktrees or update shared plans. Mark the result ready for coordinator
review and stop editing until reassigned. No Claude work is activated by this setup.

## Setup verification receipt

At preparation, the dedicated worktree was clean at the recorded starting commit.
AGENTS.md/CLAUDE.md resolved to the same instruction file. The cache copies matched
the recorded hashes and were ignored; `.env` was not copied. No NotebookLM pack or
duplicate research working folder was transferred. This is the setup receipt;
current delivery/integration refs are recorded above.

A separate worker `.venv` was installed from the existing requirements for baseline
verification; no shared writable environment or package-manifest change was used.
The fresh checkout passed **68 Python tests with four fixture-dependent skips**
and **11 Node tests**. Markdown pilot work itself requires no new package or server.
The `claude` launcher is present, but it was not invoked; its authentication and
the future session's browser tools remain to be checked at launch. No paid search,
push, merge, Claude launch or worker acknowledgement occurred during preparation.

## Codex parallel assignment

Added September 11, 2026. Task ID: `codex-empty-scrape-preservation`.
Status: **implemented and locally verified; changes uncommitted as requested**.

- Worker/coordinator: main Codex task, Troen Prospects Pipeline, in the original
  `/Users/connormurphy/Desktop/Projects/band-charter-outreach` checkout. Claude keeps
  its separate completed worktree at `e171954`, originally based on `39171e5`.
- Assignment originally recorded on `codex/research-reliability-plan`; implementation
  branch `codex/empty-scrape-preservation` is now created from
  `ed8a0dbcd29bfd83d30a3cce7589f27045bc4575`. Existing uncommitted assignment and
  setup/guide work carried forward without a commit; unrelated packs are preserved.
- Exact implementation ownership: `scripts/run_all.py` and `tests/test_run_all.py`
  only. Shared status/plan/instruction/ledger updates remain the coordinator's
  separate responsibility. Claude's four output files above are not Codex edit
  targets; neither task depends on the other's unfinished work.
- Outcome: when a live scraper returns no rows without proof of a valid empty
  result, retain its existing interim CSV unchanged and report the source as
  incomplete/no-data instead of treating it as a successful replacement. This is
  the empty-result portion of audit A03, confirmed still present in `run_scrapers()`
  and `write_interim_scoped()` during assignment review.
- Resources: temporary-directory fixtures and mocked sources only; zero network
  requests, paid calls, new dependencies, servers or production refreshes. Redirect
  all test writes, including blocked-source metadata, into temporary directories.

Required regression checks before calling the fix complete:

1. Reproduce loss with an empty live scrape and an existing source CSV, for both
   a full run and year-scoped run; require byte-for-byte preservation after repair.
2. Verify an empty first scrape does not create a misleading successful empty CSV;
   report no-data/incomplete status and do not clear it as a success.
3. Preserve existing `BlockedSource` behavior; allow another successfully mocked
   source to proceed while the empty source's records remain intact.
4. Keep successful nonempty and year-scoped replacement behavior covered, including
   history outside the requested years. Run the targeted runner suite and relevant
   Python checks, inspect the complete diff, and verify production data is unchanged.

Verified September 11: 25 runner tests pass; focused runner, scraper, news and
SerpAPI checks pass 54 tests with four missing-fixture skips. The guard also rejects
rows missing school/event/source URL, including mixed valid/invalid batches,
before any source CSV write. See [the completion record](../PROGRESS.md#latest-checkpoint--empty-scrape-preservation-fix-september-11)
for the failure reproduction, preservation evidence and limits.

This does not solve nonempty partial results with populated fields, source/year completeness, identity
joins, cache freshness, atomic writes or output reconciliation. Do not mark A03 or
the whole refresh pipeline repaired. Keep the Carnegie partner example moving
through its reviewed sources. The latest user instruction requires the current
Codex preservation implementation and documentation changes to remain **uncommitted**.
The subsequent user request separately authorized integration of the reviewed
partner commit; only that commit was cherry-picked. Do not push or relaunch Claude.
Review corrections and shared records also remain uncommitted.

**Audio overview:** defer generation and NotebookLM pack updates until project
completion. Preserve existing packs; do not make the audio a dependency for either
assignment. This coordinator addendum is conveyed through the completed startup
prompt, not by editing or advancing Claude's prepared checkout.

## Partner review and integration receipt

Reviewed `e171954439e97219491ff6fc2ff332700a1580ef` against exact base
`39171e5fe873bca7fca3f05723a8fca9985f081b`. All four paths were additions within the
assignment; no fifth file, code/data change or dependency was included. The worker
handoff reports no running process and the checkout is clean. The original commit
was cherry-picked without conflicts into the coordinator branch as
`a7ceefc139c1f075060fc9f02a0ec70a86a975d3`, preserving the prior uncommitted fix.

**Acceptance:** the internal research/draft passes with coordinator corrections
and a recorded budget-accounting finding. It is not approved for external use.
Seven MTC sources and the EPT screening page were rechecked; public identity,
staff roles, services, organizational contact and planned calendar are supported.
Affiliations/awards remain self-reported. Matching dates and NYIMF in a budget
filename do not identify Troen as the public listing's producer: that match and
any MTC/Troen relationship remain **explicitly unconfirmed**.

**Full original page tally:** MTC 7 reviewed/7 attempted; EPT 1 reviewed/1 attempted;
Fourwinds 0 reviewed/1 blocked attempt. Total **8 readable pages, 9 content-URL
attempts across 3 providers**. The eight-readable-page count does not erase the
ninth attempt. The original wording counted the selected provider; an eight-attempt
interpretation would be exceeded by one. This finding is disclosed rather than
claiming all budget checks passed. Robots requests are separate; a complete request
log was not supplied. Zero paid calls is worker-reported, and review made no paid
API call. The blocked source was not retried during coordinator review.

**Corrections in the coordinator checkout:** remove the false September-to-March
12–18-month timing inference, narrow ambiguous testimonial attribution, keep
proposed support distinct from agreed obligations, replace the likely event match
with unconfirmed identity, and remove business-confirmation prerequisites. The
worker's original delivery remains available unchanged in its commit. Questions
about event identity, relationship history and commercial responsibilities now live
in [the internal register](../INTERNAL-DECISIONS.md); no one was contacted and no
answers are needed to accept this internal draft.

**Proposed next increment at review:** connect MTC to the workbook/demo.
The user's narrower research-only scope below supersedes the partner-sheet
connection. Preserve existing manual edits and avoid the legacy refresh.
The audio overview remains deferred. Details
and verification are in [shared progress](../PROGRESS.md#latest-review--claude-partner-pilot-september-11).

## Coordinator follow-through — MTC research example

September 11: the user narrows the connection to a small illustrative research
candidate. Keep the event relationship unconfirmed; do not turn it into an
outreach strategy, contact recommendation or partner-sheet presentation.

Codex owns this sequential increment in the main checkout on
`codex/mtc-research-example`, from `a7ceefc`. Existing uncommitted changes were
carried forward intact. Exact edit scope: `demo/carnegie-hall/mtc.json`,
`review.cjs`, `build.cjs`, `build-workbook.mjs`, `index.template.html`, `index.html`,
`.generated-manifest.json`, `README.md` and the existing opportunity XLSX in that
folder; `tests/carnegie_review.test.cjs`; coordinator status in AGENTS.md, PLAN.md,
PROGRESS.md, strategy 03 and this ledger. Reuse existing styling and source evidence.

The four pilot files, Claude's checkout, preservation fix/tests, school JSON,
Word/Markdown exports, source data, packs and side-task guides remain untouched.
No new source retrieval, paid call, outreach, commit or push is included.
Acceptance: three distinct organization records with their own sources; MTC is
a candidate with an unconfirmed NYIMF/Troen match in both workbook and demo;
school records and manual edits survive; no MTC contact or pitch action appears.
Run focused tests, reconcile the workbook and HTML, and inspect both visually.

**Completion:** implemented and verified locally, uncommitted. Fifteen focused
tests pass; workbook cells/styles/panes and the original school records are
preserved, with three new evidence rows and one candidate row. Desktop/mobile
viewport and workbook renders were inspected. The original exports and all four
pilot files remain byte-identical. Source and event-relationship boundaries match
across the JSON, workbook and card. No MTC contact action or internal partner-sheet
link was added. See [the implementation record](../PROGRESS.md#latest-implementation--mtc-research-example-september-11).
No new task is assigned to Claude. A short event FAQ is suggested for later;
audio remains deferred. No commit or push occurred.
