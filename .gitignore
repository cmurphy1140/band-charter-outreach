.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.env
.env.*
!.env.example
.DS_Store
data/raw/*
# SerpAPI responses are tracked: each one costs monthly quota and holds no key.
!data/raw/serpapi.com/

# Disposable static hosting output and local Vercel settings
/dist/
/.vercel/

# Agent worktrees created by the harness. Each is a separate git checkout used by a
# background worker; they are scratch space, never project content.
.claude/worktrees/
